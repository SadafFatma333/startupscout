# -----------------------------
# Application Environment
# -----------------------------
ENV=dev                      # dev | prod | test
EMBEDDING_BACKEND=minilm     # minilm | openai

# -----------------------------
# Database Configuration
# -----------------------------
DB_NAME=startupscout
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432

# -----------------------------
# API Keys
# -----------------------------
OPENAI_API_KEY=your_openai_api_key_here

# -----------------------------
# Logging / Monitoring (optional future use)
# -----------------------------
LOG_LEVEL=INFO
SENTRY_DSN=

# -----------------------------
# Paths (optional overrides)
# -----------------------------
DATA_DIR=./data
